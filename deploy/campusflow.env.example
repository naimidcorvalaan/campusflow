# Copy to a root-owned file such as /etc/campusflow/campusflow.env (mode 0600).
# Never commit the populated file.
TJU_LLM_API_KEY=replace_on_server
TJU_LLM_BASE_URL=replace_with_project_endpoint
TJU_LLM_MODEL=tju-llm
CAMPUSFLOW_DATA_DIR=/var/lib/campusflow
