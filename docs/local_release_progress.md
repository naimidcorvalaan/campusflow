# 本地比赛交付进度

起点：main / `37adb17a05fee5f89dfacdd283a4268781c8e253`，工作区干净。只做本地交付与比赛包装；不调用真实模型，不操作真实档案，不提交、推送、打tag或创建远端Release。

## Round 1

- 已读当前启动器、README、依赖、演示入口与相关回归。
- 开发环境：Windows / Python 3.8.10 / Streamlit 1.31.1；依赖保持原版本。
- 首次安装演练：独立系统临时目录 `campusflow-release-*`，原开发venv未动。首次受限网络环境600秒超时；批准联网后同一环境安全重试，完整依赖安装135秒成功。
- 已复核比赛官方提交页：README、DESIGN、演示视频或截图、填写后的附件2版权合规承诺书为要求。已补根目录DESIGN；承诺书仍待作者完成，不能代替作者承诺。
- 已定位需要收口：启动器关闭时子进程生命周期；安装无阶段信息；README混入开发历史；演示脚本中的控件标签已过时。
- 决策：比赛推荐源码压缩包＋一键启动，不分发不可搬移的venv，不做exe PoC；保持回环监听。

## Round 1完成

- 安装阶段输出准备/下载/缓存/安装；30秒无新阶段显示实际等待时长；10分钟有界停止。不打印软件源URL、凭据或原始异常。
- 修复关窗后页面子进程可能遗留：Windows Job Object绑定pip/Streamlit生命周期。实际OS回归用父进程突然退出验证子进程被结束；实际关闭本次启动器后8542/8543监听消失。
- README按产品差异、快速开始、校园规划/执行反馈/原始通知三幕重写。旧README详细内容完整保留为technical_reference，根目录DESIGN提供技术入口。
- 准备CHANGELOG、官方比赛清单、本地源码zip构建器；仅本地生成，不创建远端Release。
- 第一轮正式Streamlit控件走查完成：首页→规划→反馈→材料→保存设置→刷新恢复；最低模型响应为合成数据。

## Round 2：评委新电脑式演练

从本地生成的源码包解压到另一个全新目录，目录中没有venv、真实.env或用户数据库。
使用已有Python3.8.10，通过中文bat实际启动（开发参数仅指定离线演示和独立端口）。

| 检查 | 实际结果 |
|---|---|
| 创建新venv→真实pip安装→Streamlit就绪 | **完整通过**；这次pip阶段116秒，最后进入8543就绪状态。默认浏览器打开调用实际执行，但系统默认浏览器前台标签没有单独截图核验 |
| 时间与缓存边界 | 116秒不是冷缓存保证；pip复用了前次缓存并完成依赖安装。整体链路未独立计时，不把pip秒数写成下载到使用的总耗时 |
| 第二次日常启动 | 输出“运行环境已就绪，无需重复安装”，直接启动；未再运行pip |
| 服务运行时重复启动 | 输出“已在运行，正在打开原页面”，没有另起服务 |
| 关闭启动器 | 本次启动器进程及对应端口监听均消失；没有结束其他用户服务 |
| 进程重启恢复 | 指定同一个隔离rehearsal目录，重新打开SQLite与全新浏览器；作业剩余40分钟方案、课程及保存偏好仍在 |
| 缺配置的正常入口 | 新解压包没有.env，进程局部配置留空；正式src入口8544能打开并展示复制/填写/重启指引，没有点击模型操作 |
| 缺Python/错误版本 | 真实bat限制PATH回归与版本门禁通过；拒绝3.9.7，不改变生产Streamlit |

第二轮使用新交付目录再次完成同一10项浏览器观察：1440×900首页/方案/反馈/材料/设置/恢复，
1024×768和390×844方案/设置。实际Edge headless视口模拟，不称为手机实机测试。
没有横向溢出；桌面方案双卡底部差小于2px；设置可保存、关闭和恢复。
本轮没有需要继续扩大范围的产品问题。两次观察中的脚本标签/断言等待问题已校正，不伪称为产品缺陷。

## 交付证据

- 根目录screenshots保留5张正式渲染截图；合成身份和材料，无真实个人信息。
- artifacts/release_preview保存10项测量JSON、响应式/恢复/缺配置证据，已忽略；本地zip输出到artifacts/releases，已忽略。
- 相关focused：**19 passed in 2.01s**（启动器、源码包、真实反馈演示接线、低输入旅程）。
- 唯一一次最终full：**2268 passed in 118.98s**。
- `compileall -q src tests scripts`、`git diff --check`通过。full之后只补文档记录/本地包，不再修改业务代码。
- 8份主要文档共27个本地链接核验，无缺失。
- 本轮变更文本敏感特征扫描未发现命中；打包清单311文件，无真实.env/数据库/日志/pyc，包含.env.example及5张截图。未读取真实Key。
- 起点干净；本轮11个已跟踪文件修改、16个新增文件（含5张图片），无暂存/提交/推送/tag操作。地图、生产依赖、用户真实数据库未改。

## 剩余事项与停止

1. 作者按官方附件完成版权合规承诺书，核对数据/素材分发依据及参赛仓库Internal可见性。
2. 真实TJU文本服务的低输入/反馈/材料理解质量仍待集中验证；本轮所有产品请求为fake。
3. 图片能力仍未真实验证、未开放，不宣传支持截图；不阻断文字版准备。
4. Python3.12为文档推荐；本机实际安装演练使用3.8.10，未声称覆盖所有Windows/Python组合。

首次完整安装已不再是未完成项。源码包＋一键启动为比赛推荐；不做exe PoC，理由见比赛清单。
完成两轮后停止，不进入图片、公网、认证或其他新阶段。
