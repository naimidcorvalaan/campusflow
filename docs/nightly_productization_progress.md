# CampusFlow 夜间产品化冲刺

## 起点与边界

- 2026-09-06；main；HEAD `446397805f68899cfd40f283d4e9c1271716de32`。
- 工作区干净，本地上游记录一致；没有 fetch，不代表实时核验远端。
- Python 3.8 / Streamlit 1.31.1；不升级生产依赖。
- 不调用真实 API、不读取密钥、不修改地图、不操作真实档案、不提交或推送。
- 上一包记录见 `nightly_product_upgrade_progress.md`；本文件记录本次新增工作。

## 当前阶段

交付收尾完成：两轮完整浏览器流程、第三轮局部 polish、失败/恢复检查与最终截图复查通过。不继续扩功能。

## 已发现的具体缺口

- 旧离线入口只调用展示 helper，不能串起完整 live 保存/恢复流程。
- 旧演示的 settings 视图每次 rerun 强制打开面板，无法正常关闭。
- 旧 demo guide 仍把 P1 称为当前入口，与现在产品不一致。
- 上一包未得到可用窄屏截图；本次发现已有独立 Node / Playwright，可直接使用，不安装生产依赖。
- 实际桌面：顶部被默认工具条遮挡、主输入标题重复、反馈表单在空方案时出现、当前行动被输入区挤到下方。已修层级，已有方案时收起重新输入，唯一正式方案先于反馈。
- 实际课表导入后：提前 rerun 导致未渲染的主校区 widget 被 Streamlit 清理，页面退回选校区。已在渲染前保留已知主输入 widget 值；不更改校园或规划事实。
- 代码复现：改粘贴材料后旧预览仍可确认；无效 enum/周次导致预览控件异常。已增加材料指纹守卫和可编辑缺失态；严格验证仍在。
- 设置保存/关闭可见操作区、键盘焦点约束与 Escape 关闭已接线；日期输入补中文可访问标签。待最终浏览器验证。
- 更新失败旧可靠计划虽在状态里，但提前 return 当次不展示。已在失败路径恢复展示同一可靠 bundle。
- 69 项 focused 通过（正式初始→预演→采用、失败保留、课表真实识别→导入→新 SQLite 连接恢复等）。
- 首轮实际 Edge 流程：初始规划→设置/课表导入→刷新→顺序预演/采用→估时采用30分钟/加入→重开恢复，成功。1440/1024/820/390 宽度均实际加载；无横向溢出。
- 跨流程复现并修复：refresh 使用旧 reservation base，未注入最新设置课程；What-if 随后又为已结束课程生成过去的移动，导致 meal 降级。现在刷新对 base 注入当前设置，execution timeline 只跳过有确定结束且已结束的课程（canonical 记录不删，未知结束不跳过）。实际 live/import/refresh/preview 回归验证 meal40与两段路线保持。
- 第二组 focused：92 passed（产品边界、live、估时、个人设置）；执行序列相关31 passed。尚未跑本包 full。
- 使用 Streamlit 1.31 已有原生 theme / viewer toolbar 配置统一标签/checkbox，未升级依赖；手机品牌按钮保持同排，原生编辑控件不变。
- 估时结果分开显示“粗略专注用时/建议用于规划”，默认只需确认采用分钟，可按需展开修改范围；已加入状态移除重复加入按钮。
- 新增测试运行安全边界：收集前禁止 dotenv 读本机凭据，测试期阻断 requests 网络并使用临时档案；不改变生产客户端。
- 第二轮（全新临时档案）：设置→课表导入→初始方案→估时/手改30分钟→加入→预演/保持→再次预演/采用→重开恢复，全程实际原生控件操作通过。mock 共46次，包含多次明确业务操作，并非“一次反馈46次”；采用无增加，重开后的新会话0次。真实模型0次。
- 真正的响应式观察：1440×900、1024×768、820×1180、390×844 均拿到可用成图。桌面左右卡上下边误差<2px；390单列自然高度，无横向溢出。视口模拟不等于实机。
- 第三轮发现并修复：首次无方案估时成功后展开区重新挂载导致结果被收起；增加仅会话内展示状态，不进入业务或持久化事实。空方案估时→补充重估→采用30→首份正式方案→重开，已实际通过。
- 第三轮发现并修复：吸顶样式原本施加在原生容器内部，受外层等高包装限制而失效。改为外层容器吸顶。手机深滚动后关闭按钮实测 y=18.8px，可点击；Esc关闭和返回入口焦点通过。面板内保存/导入反馈不再藏到遮罩背后。
- 820竖屏的半屏预览把“星期二”挤成省略号；改为约90%宽的覆盖面板，桌面仍48%，手机全宽。设置分区由原生 tabs 保留，辅助脚本仅恢复当前分区/键盘焦点，不拥有输入或调用模型。
- 新增失败回归先失败再修复：恢复旧计划后 `_prepare_restored_replan` 在调用模型前已改参考时间/当前位置；调用失败只保住bundle，没有撤回canonical准备事实。现在复用既有业务快照撤回失败准备；保存失败的预演采用也继续显示原可靠方案。未改 allocator 或并行/进度语义。
- 课表名称被清空不再静默删课，明确要求补全或删除条目；用户数据保持。UI异常诊断只保留阶段/异常类型，避免原始异常载荷混入日志。
- 最后关键 focused：141 passed + What-if/live 11 passed（两个不重复集合）；此前客户端/估时/导入/表达62 passed。最终 full 尚待执行。

## 第二轮筛出的高价值问题（实际证据，不凑数量）

1. 估时首份结果被收起（浏览器）——已修。
2. 设置保存操作栏滚走（浏览器测量）——已修。
3. 设置导入反馈位于遮罩背后（浏览器）——已修。
4. 820竖屏课表字段省略（截图）——已修，待最终截图复查。
5. 恢复后失败更新留下新参考事实（实际live回归）——已修。
6. 无名称课表条目被忽略（保存路径校验）——已修。

未扩大到任务库、账号、地图、allocator 或新模型角色；没有为页面文案反向重排。

## 计划与证据

1. 实际浏览器首轮：空状态、设置/导入、估时、方案/预演、恢复/失败。
2. 依据观察修复高价值问题；集中 UI 层级、草稿状态、操作反馈。
3. 干净临时档案第二轮完整操作；四个视口观察与克制 polish。
4. focused → 一次 full → compileall / diff-check；更新演示指南、截图和交接。

## 下一步

保存本次未提交代码与开发证据，按 `demo_guide.md` 可重复排练。正式服务与实机效果仍待观察；不自动进入下一阶段。

## 最终验证与交接

- 集成 full：`pytest tests/ -q` → **2190 passed in 56.82s**。本包只跑一次最终 full。
- full 后仅调整设置键盘焦点展示接线：关闭时 iframe 原先随面板销毁，无法可靠返回入口；辅助桥移到始终挂载的顶部展示层，保留有界焦点恢复，不新增业务或模型调用。
- 上述最终改动后 focused：`test_productization.py`、`test_p2_live_main.py`、`test_personal_settings.py` → **83 passed in 6.44s**。此前集成 focused 141 + 11 passed；各批次不相加冒充独立测试总数。
- 最终 `compileall -q src tests` 与 `git diff --check` 通过。Git 仅提示已有 LF/CRLF 策略，非空白错误。
- 最终浏览器 `demo` 恢复分支完成：设置打开/切区/Esc关闭/焦点返回（0调用）→初始→估时→手改30分钟加入→预演保持→再次预演采用→重开；结果写入 `demo-measurements.json`。此前两轮完整流程包含首次设置和课表识别导入；补查包括无方案估时创建、失效重估、过期恢复、网络/格式失败。
- 实际 Edge 模拟视口：1440×900、1024×768、820×1180、390×844。最终长时间线桌面两卡 top/bottom 相等；390 页面 scrollWidth=390，右卡自然高度约94px，不再强撑。短方案无提示内容时不造右卡。
- 820课表面板实际宽740px，星期/时间完整可编辑；390深滚动保存/关闭仍可见。原生日历 Escape 不误关面板，切区和导入后保持原分区。不是 iPad/Safari/手机实机测试，也不是完整无障碍认证。
- 截图位于 Git 忽略的 `artifacts/nightly_ui_preview/`，全部为 synthetic demo：`demo-first-use-1440.png`、`demo-plan-1440.png`、`demo-settings-1440.png`、`demo-import-820.png`、`demo-estimate-1440.png`、`demo-whatif-1440.png`、`demo-restored-plan-390.png`。过期/失败/短方案证据为 `polish-*`。

### 调用、数据与资源

- 没有新增生产模型角色或调用路径。估时与课表识别沿用通常1次、格式修复至多再1次；规划/反馈/预演沿用已有有界链；编辑、保存、关闭、采用、保持、恢复0调用。所有开发模型响应为 fake，未调用真实服务。
- 不迁移数据库 schema，不接触真实档案。临时数据库新连接及服务重启恢复已观察；测试现在在收集前禁用旧客户端自动 dotenv 读取，并拦截未mock HTTP。没有打开或输出密钥值。
- 本轮沿用仓库原创校园路径装饰与北洋蓝灵感，不新增校徽/地标素材，不把产品配色称为官方标准色。既有来源记录见 `nightly_product_upgrade_progress.md`，图片能力说明见 `image_task_estimation_capability.md`；不依赖新增远程字体或热链。
- 图片协议适配仍为已有能力；精确学校部署的图像支持没有新增可靠依据，保持关闭，真实读图未验证。未修改地图、生产依赖、模型服务地址、分块/并行授权或实际进度语义。

### 修改范围与提交草案（未执行）

- 产品展示：`campusflow_ui.py`、`p2_live_main.py`、`p2_main.py`、公开 `.streamlit/config.toml`。
- 局部闭环修复：`p2_session.py`、`p4_execution_movement.py`、`p5_what_if.py`、`timetable_text_import.py`。
- 开发证据：离线入口/fake caller/浏览器检查脚本、3个相关测试文件、README、本进度与演示指南。
- 工作区17个修改/新增文件，无暂存、无提交、无推送。真实 `.env`、数据库、截图产物已有忽略规则；新增配置仅公共视觉和上传大小，不含凭据。

建议标题：**产品化：统一校园交互，打通课表、估时与离线演示闭环**

建议正文：统一首次使用、主方案、估时与半屏设置交互；修复导入后输入状态丢失、过期预览、设置刷新课程接线及恢复失败回退；补齐真实live离线演示与四视口观察。集成2190 passed，最终展示焦点调整后相关83 passed，静态检查通过。真实模型及设备实机未验，不宣称所有线上场景完成。

### 尚未解决 / 本轮停止边界

1. 外部能力限制：学校精确部署的图片支持无可确认依据，保持关闭。
2. 真实环境待观察：真实Qwen理解/估时质量、用户档案上的运行、Safari与手机/iPad实机未验证；离线mock不替代这些证据。
3. 技术维护边界：Streamlit 1.31没有原生drawer，本地辅助脚本依赖限定的组件标记与原生tab/dialog；升级Streamlit时需复核。本轮不升级、不建设通用组件库。

## 2026-09-06 图片能力边界与信息架构收尾

- 开始状态：`main` / `ad73d23f672713af33daf3adcff8602f497633a4`，工作区干净；本轮不提交、不推送。
- 当前非敏感模型标识 `tju-llm`；现有客户端已具备 Data URI / `image_url` 构造。按用户授权只发送一次合成 PNG，执行通道没有返回可观察响应或异常，无法证明读图；未使用用户课表图探测，也没有第二次请求。图片入口保持关闭。
- 首页改为默认北洋园；品牌副标题固定为“tjuer专属的校园时空规划系统”，去掉首页非官方徽章与泛化口号。首屏用一条紧凑能力带说明本地校园路线、持续调整、估时确认和持久设置；主输入仍优先。
- “更多设置”改为“时间设置”；主输入、反馈、估时补充和长期备注改为自然问题与 placeholder。未确认图片时不再展示上传控件或长篇开发说明，只提示使用文字。
- 个人默认：步行、希望适当穿插休息。步行仍受本次明确方式覆盖；首次真正使用默认步行的移动旁显示一次轻提示，并以个人设置中的非规划标记持久化，不改变设置 revision 或正式计划事实。
- 常用地点只编辑一个当前校区，只保留“宿舍 / 常用学习地点”两个选择框；候选严格使用地图 category（dormitory 与 teaching/teaching_building/library）过滤，未改地图、坐标或路线。旧未匹配记录保留为可替换状态。
- 持久化解码允许 dataclass 新增带默认值字段，未知字段及缺失必填字段仍拒绝；已有SQLite记录无需清空。
- 图片任务估时和课表截图识别未正式启用；文字估时、文字课表识别/预览/导入继续使用原闭环。用户提供的真实课表截图未写入仓库、日志或探测请求。
- 旧档案若曾把出行方式或学习节奏保存为“未指定”，运行时也会采用新的步行 / 适当穿插休息产品默认；旧未匹配地点保持未匹配，不会在保存时越过角色筛选偷偷绑定。
- 离线正式渲染走查完成：1440×900 首页、个人设置、常用地点、课表导入、当前方案；1024×768 与 820×1180 课表/时间线；390×844 课表、估时和恢复方案。页面横向宽度均等于 viewport；1440/1024/820 双栏等高，390 自然单列。截图与测量位于 `artifacts/nightly_ui_preview/final-*`（开发产物，不纳入 Git）。
- 针对性回归最终 99 项通过；全量第一次仅暴露旧测试仍假定校区下拉含空占位，更新为验证默认北洋园后，最终全量 2197 项通过。`compileall -q src tests` 与 `git diff --check` 均通过。

## 2026-09-06 多人数据隔离与部署准备第一版

- 开始状态：`main` / `ad73d23f672713af33daf3adcff8602f497633a4`；保留上一包 13 个既有未提交文件，本轮未提交、未推送、未调用真实模型、未修改地图。
- 新增薄身份边界 `ProfileIdentity`：规划与业务层只持有随机内部 `user_id`、是否持久化及档案类型，学号只在 SQLite 档案目录中映射。匿名会话不打开或读取个人档案。
- SQLite 升级为 schema v3：计划/任务进度/固定安排/执行上下文/并行授权/可靠 final turn 继续作为完整方案快照保存，估时与个人设置分别保存；所有表以 `user_id` 分区，revision 冲突也按档案隔离。v1/v2 单用户记录事务迁入 `legacy-local`，不绑定真实学号，只允许用户明确复制到空个人档案。
- 个人设置新增“开启个性化 / 学号 / 打开档案”闭环；档案切换先保存当前可靠档案、完整读取目标，再清理用户事实并发布。未保存设置或课表导入草稿会阻止切换；失败保留当前身份与状态。学号只作档案选择，不是认证，不发送给模型或普通日志。
- 数据目录统一：显式 `CAMPUSFLOW_DATA_DIR` 优先，随后使用 Windows/macOS/Linux 平台数据目录；不落入源码工作目录。新增离线部署检查与简洁部署边界文档，未增加 Dockerfile，避免把尚无真实认证的版本误当作可安全公网开放。
- 定向回归最终 110 项通过；整包 full **2208 passed in 57.26s**。两独立浏览器上下文建立 A/B 档案，B 看不到 A 设置；第三个 390×844 新上下文重新选择 A 后恢复 A，三者 mock 调用均为 0。1440×900 还实际观察了旧本机档案的显式迁入选项。浏览器均使用仓库外临时数据库和离线模型拦截，不接触真实档案。
- 部署诊断在 Python 3.8、仓库外显式数据目录和占位配置下通过；只报告变量是否存在，不打印值。最终 `compileall -q src tests scripts` 与 `git diff --check` 均通过；后者仅有仓库既有 LF/CRLF 提示，没有空白错误。
