# 正式展示层收口验收

基于 `main` / `8dcaa9fc29c1f69ce720f14cdc6cc4ef43fd64d6` 的未提交工作区继续完成。此次只整理展示文案、页面标题与共享 CSS；已确认的路线方向、校园纹理和连续时间线结构保留。

## 文案与标题

Sidebar 保留导航名称；主区域删除重复的品牌/页面 breadcrumb。今天以当前行动或“接下来有什么安排？”为主标题，时间线、任务估时、首次配置各有一个主标题，设置抽屉有独立标题。保留既有控件、key、回调和隐藏区域挂载方式。

| 旧 | 新 / 处理 |
| --- | --- |
| CampusFlow / 今天 + 今日工作台 | 删除重复页眉 |
| 难以估计任务时间？让 CampusFlow 帮你估 | 任务估时 |
| 填写任务、时间要求和固定安排。 | 删除，输入框已有示例 |
| 今天还有什么事？ | 删除重复的输入区标题 |
| 只有包含通勤时才需要确认位置。 | 删除 |
| 计划用时不代表已完成。进度以实际反馈为准。 | 删除 |
| 安排依据 / 安排提示 / 结束鼓励语 | 删除常驻的规划解释与泛化鼓励 |
| 宿舍只是常用目的地，不会被当成你当前所在的位置。 | 删除 |
| 课表按规划日期和教学周展开；保存本身不会立刻重排当前方案。 | 删除；保存后的状态提示更新方案 |
| 设置已保存到个人档案；当前方案尚未更新。 | 设置已保存 · 请更新方案。 |
| 原方案仍保留在下面。输入没有清空，可以修改后再试。 | 删除重复失败说明 |
| 这次调整没有成功，原可靠方案仍保留。可以保留输入，稍后重试。 | 调整未完成 · 原方案和输入已保留，请重试。 |
| 这里只估目前识别到的部分，完成整项任务可能需要更久。 | 仅估算已识别内容 |
| 根据目前识别到的内容，先估算 | 专注用时 · 初估 |
| CampusFlow 整理出了 N 项 · 尚未加入 | N 项待确认 |
| 确认后会一次加入选中事项并更新当前方案；还不知道用时的任务会保留待估算，不写成已经完成。 | 删除；保留逐项确认与待估算状态 |
| 这可能是你已有的“…” ，不会自动合并。 | 可能与“…”重复 · 请选择如何处理。 |
| 有信息与已有任务不同，请只选择发生冲突的字段。 | 信息有冲突 · 请选择采用值。 |
| 地点锚点（可选） | 匹配校园地点（可选） |
| 配置保存在本机用户目录，与个人档案分开。保存不会发起模型请求。 | 删除常驻说明 |
| CampusFlow 使用模型服务理解任务、估计工作量并生成规划。首次使用请填写自己的 TJU 模型配置。 | 请填写你的 TJU 服务地址和 API Key。 |

估时依据、具体假设保留在“估时依据”折叠区，便于用户核对或调整用时。具体等待信息与部分范围提示保留在结果旁。常驻的泛化规划理由不再展示，底层生成与校验结果仍保留。

## 保留的行动提示

- 部分识别、任务内容缺失、相对日期待确定、地点待匹配。
- 材料或范围已变化时重新估算；信息冲突与疑似重复时选择采用值。
- 未保存到个人档案、设置保存后更新方案、失败时输入/草稿/原方案保留。
- 未采用的 What-if 预览，以及已采用调整的经校验生效回执。后者是操作结果，不作为泛化解释删除。
- 模型连接缺项、环境配置锁定字段的修改入口、连接测试会发送请求。
- 学号选择本机档案与身份认证的区别，保留在用户输入学号的位置。
- 默认步行的首次提示仍指向个人设置；状态条件未修改。

## Typography 与 CSS

一套本地字体栈：`Bahnschrift, Segoe UI, Microsoft YaHei, PingFang SC, sans-serif`。数字使用等宽数字特性，不加载在线字体。

| 语义 | CSS 变量 | 字号 |
| --- | --- | --- |
| 次级文字 / 状态 / 单位 | `--cf-type-meta` | 12px |
| 正文 / 控件 | `--cf-type-body` | 14px |
| 模块标题 | `--cf-type-section` | 18px |
| 页面 / 当前行动标题 | `--cf-type-title` | 30px |
| 当前行动大数字 | `--cf-type-display` | 72px；手机 62px；中等宽度 56px |

字重收敛为 400 / 500 / 600。小地图 SVG 地点标注保留 16 个 viewBox 单位的内部比例，不另创页面文字层级；它与页面使用同一字体栈。地图节点、路径投影、线宽、校园色值不改。

CSS 从历史散落的 96 个十六进制色值收口为 30 个调色值；阴影与遮罩从共用墨色派生。合并 7 处同选择器重复规则及早期高优先级补丁，删除 14 条重复响应式声明。原来的压缩 CSS 展开为可审阅声明，行数增加不表示增加了页面组件。

移除：`cf-workspace-header`、`cf-empty-intro`、`cf-input-help`、`cf-feedback-help`、`cf-side-hint`、`cf-section-eyebrow`、`cf-summary-heading`、`cf-settings-help`、`cf-settings-unsaved`、`cf-setting-note`、旧 `.cf-quiet button`、旧估时器 `cf-estimate-name / cf-estimate-row / cf-estimate-help`。

上传区隐藏规则从 `material_ui.py` 内嵌 CSS 迁入 `workspace.css`，保留原生上传输入与挂载标记。仍在使用的 action、侧栏信息、估时结果和 What-if 样式继续保留。

## 核对结果

- Focused：269 passed。
- Full：2612 passed，106.02 秒。
- `compileall src tests scripts`：通过。
- `git diff --check`：通过。
- 浏览器：1440 / 390，42 个状态、226 项检查全部通过；另有 4 张独立路线截图。
- 检查无横向溢出、字体栈一致、页面字号在五个语义层级内、无重复说明、无脚本错误或外部页面请求。
- 已发布计划在切页和失败后保持一致；路线生成文件逐字节未改，连续时间线事实投影函数 AST 未改。
- 本轮开始快照中的 137 个其他源码/数据文件未改；全部原生控件 key 表达式一致。
- P5 测试继续验证校验后的叙述、时刻和 exact candidate 发布；仅将已删除的常驻说明断言移到其保留的结构化结果。

测试均使用 mock 和已有数据。浏览器通过正式 `p2_live_main.main`，注入离线响应与隔离目录；真实模型/地图 API 未调用。工作区未 commit / push。

完整机器记录位于 `artifacts/presentation-cleanup/`：`verification.json`、`scope-check.json`、`css-audit.json`、`copy-changes.json`、`focused.xml`、`full.xml`、`after/observations.json`。修改前截图另存于 `before/`。

## 最终公开素材

今天、时间线、任务估时和设置统一使用 [README 正式图片](assets/readme/)，最终标准见 [前端冻结](FRONTEND_FREEZE.md)。阶段性状态截图及一次性浏览器检查脚本仅保存在本地 ignored artifacts，不属于公开产品资产。
