# v1.0.0-rc1 Windows 发布验收

前端冻结提交：`4dc5377a3b9d505592679d21e93dfed25d9d0a83`。正式前端与业务源码保持冻结。

## 发布包

- 文件：`CampusFlow-v1.0.0-rc1-windows.zip`
- 单一顶层目录：`CampusFlow-v1.0.0-rc1/`
- 153 个文件，压缩 587,146 字节，解压 2,221,109 字节。
- SHA-256：`766c2c4f600d7cc490343cd5e99ca67f3a410ec92a8248d569b67fc07cd5d9d9`
- 使用 `python scripts/build_local_release.py` 在 ignored `dist/` 构建，已有目标不会被覆盖。
- 白名单包含正式源码、CSS、三份本地地图 JSON、启动器、六项运行依赖、开发者配置模板和使用说明。
- 不含测试、prototype、截图、验收脚本、Cloud 资料、环境、数据库、上传材料、缓存或真实密钥。

## 独立运行

Windows / 64 位 Python 3.12.14，从新临时目录解压，通过中文批处理入口创建全新 `.venv`。
不复用仓库环境、`.env`、数据库或 Python 缓存；依赖安装可复用 pip 下载的 wheel 缓存。
模型配置和运行数据使用隔离的本机用户目录。HTTP 模型替身运行在回环地址，没有 TJU / DeepSeek / 地图服务调用。

| 检查 | 结果 |
|---|---|
| 首次启动到服务就绪 | 113.58 秒，其中依赖安装 97 秒 |
| 关闭后第二次启动到服务就绪 | 6.32 秒，跳过依赖安装 |
| 重复双击 | 打开已有页面，无第二份服务，退出码 0 |
| 全新配置 | 首次 TJU 配置出现；保存不发模型请求，随后可立即规划 |
| 重启配置 | 从隔离的 `%LOCALAPPDATA%\CampusFlow\model-service.json` 读取，不再询问 |
| 卫津路 | 图书馆 → 学三食堂，490 米，步行约 7 分钟 |
| 北洋园 | 郑东图书馆 → 第五学生食堂，216 米，步行约 3 分钟 |
| 包内路网 | 北洋园 129 节点 / 404 边；卫津路 251 节点 / 168 边；与源数据逐字节一致 |

首启、二次启动和页面使用均为正式客户端与正式计划链路；模型响应由本机 fake 服务提供。
最终包与独立启动验收的运行文件逐字节一致；最后仅规范化使用说明末尾空行。

## 检查与修复

- Builder / startup：**38 passed / 0 failed**。
- 新增 Windows 非 UTF-8 控制台真实批处理回归。原脚本在跳转到中文提示之后的标签时会误读字符；将共同结束标签放在中文提示前，保留原启动能力与中文提示。
- 仓库及解压环境 compileall、pip check、git diff --check 通过。
- 配置模板中未加引号的真实凭据值也会在构建前被拦截。
- README 本地链接与图片、ZIP 白名单 / CRC / 密钥与用户文件扫描通过。
- 正式业务和展示源码没有修改，不重复运行 full；冻结基线为 **2615 passed / 0 failed**。
- 验收日志和临时截图保存在 ignored artifacts；公开仓库保留 6 张最终正式截图，prototype 仅保留源文件与设计归档。

除明确 bug 外，不建议继续整体视觉优化。
本次只完成本地提交与 RC 文件，不 push、tag 或创建 GitHub Release。
