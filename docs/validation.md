# 公开验证摘要

本次公开源码同步基于开发检查点 `7e6f34d`，定位为可运行、可展示的工程原型，不是所有产品场景已验收的生产版本。

## 证据的范围

| 验证 | 结果与解释 |
| --- | --- |
| 基线自动测试 | Full 3,858 passed；相关 focused 290 passed；编译与差异检查通过。使用 fake/mock 和隔离数据。 |
| 通用后台场景 | 一次同版本下载、训练、充电、监督、互斥、返程和剩余时间反馈共7/7产品审查通过。不是任意后台场景的准确率承诺。 |
| 历史困难七例重复 | 前一候选三轮技术21/21、产品20/21；随后继续修复，未与其他版本拼成最终通过率。 |
| 最新两例真实抽查 | 技术2/2、产品1/2。后台过程顺序正确；另一例时间线正确，但摘要把用户已在的宿舍写成“前往”地点，仍判产品失败。 |
| 完整产品验收 | 原40例＋新增40例尚未在最终版本全部执行，不宣称80/80。 |

产品评审先冻结输入事实与期望，再只看用户可见输出、行动和时间线，最后查看内部诊断。schema、HTTP或自动测试通过不自动等于产品通过。

## 仍存在的边界

复杂自然语言、来源选择和材料理解存在模型随机性；有界修复可能失败，系统需要保留旧方案或明确失败。上述摘要地点措辞是已知限制，当前仍以正式时间线为事实来源。材料容量与校园覆盖见 README。

本次公开仓库复验：Full **3,858 passed**，Focused **313 passed**；compileall 与 diff check 通过。Windows启动器、首页、今日计划表、任务估时及首次配置页通过隔离示例浏览器复验，未调用真实模型。公开仓库不包含真实用户数据、原始模型响应、凭据或内部评测材料；合成 fixture 和离线回归测试用于复现工程约束。
